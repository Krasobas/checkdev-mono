apiVersion: apps/v1
kind: Deployment
metadata:
  name: cd-generator-dpl
  namespace: checkdev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cd-generator
  template:
    metadata:
      labels:
        app: cd-generator
    spec:
      containers:
        - name: cd-generator
          image: checkdev/cd_generator:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9903
          envFrom:
            - configMapRef:
                name: cd-app-config
            - secretRef:
                name: cd-db-credentials
            - secretRef:
                name: cd-app-credentials
          env:
            - name: SERVER_PORT
              value: "9903"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://cd-postgres-svc:5432/cd_generator"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9903
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9903
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: cd-generator-svc
  namespace: checkdev
spec:
  selector:
    app: cd-generator
  ports:
    - port: 9903
      targetPort: 9903